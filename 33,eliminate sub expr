#include <stdio.h>

int main() {
    int a = 5, b = 3, c = 7;
    int x, y, z;

    x = a + b * c;
    y = a + b * c;
    z = b * c;

    printf("x: %d\n", x);
    printf("y: %d\n", y);
    printf("z: %d\n", z);

    return 0;
}
