#include <stdio.h>

int main() {
    int x = 5;
    int y = 10;
    int z;

    if (x > 0) {
        z = y + 5;
    } else {
        z = x - 5;
    }

    printf("Value of z: %d\n", z);

    return 0;
}
